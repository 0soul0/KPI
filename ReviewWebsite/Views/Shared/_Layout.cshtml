<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - ReviewWebsite</title>
    @Html.Partial("LoadCSS")
    @await RenderSectionAsync("Css", required: false)
</head>
<body>
    <div class="">
        <main role="main" class="container-fluid text-center m-0 p-3">
            @RenderBody()
        </main>
    </div>

    @Html.Partial("LoadJS")   
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
